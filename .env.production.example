# ═══════════════════════════════════════════════════════════════════════
# DocWise — Production Environment Variables
# Copy to .env and fill in real values before deploying.
# ═══════════════════════════════════════════════════════════════════════

# ─── PostgreSQL ──────────────────────────────────────────────────────
POSTGRES_USER=kagaz
POSTGRES_PASSWORD=CHANGE_ME_use_a_strong_password

# ─── MinIO (S3-compatible storage) ───────────────────────────────────
MINIO_ROOT_USER=CHANGE_ME_minio_access_key
MINIO_ROOT_PASSWORD=CHANGE_ME_minio_secret_key

# ─── Public URLs (replace YOUR_DROPLET_IP with your server's IP) ────
# MinIO presigned URLs are proxied via Nginx at /storage/
MINIO_PUBLIC_ENDPOINT=YOUR_DROPLET_IP/storage
NEXT_PUBLIC_API_URL=http://YOUR_DROPLET_IP/api
CORS_ORIGINS=["http://YOUR_DROPLET_IP"]

# ─── Azure OpenAI — Chat Model ──────────────────────────────────────
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-5-mini
AZURE_OPENAI_DEEP_DEPLOYMENT=gpt-5.2-chat
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# ─── Azure OpenAI — Embedding Model ─────────────────────────────────
AZURE_OPENAI_EMBEDDING_API_KEY=
AZURE_OPENAI_EMBEDDING_ENDPOINT=
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-large
AZURE_OPENAI_EMBEDDING_API_VERSION=2024-12-01-preview

# ─── Clerk Auth ──────────────────────────────────────────────────────
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_JWKS_URL=
CLERK_ISSUER=
